import React from 'react';
import RadicalList from '../../containers/RadicalList';

const RadicalTimeline = ({props, currentUser}) => {
    return <RadicalList currentUser={currentUser}/>
};

export default RadicalTimeline
