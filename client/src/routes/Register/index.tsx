import React from 'react';
import RegisterForm from '@/components/features/auth/Register';

const RegisterPage: React.FC = () => {
	return <RegisterForm heading="Join us!" buttonText="Sign up" />;
};

export default RegisterPage;
