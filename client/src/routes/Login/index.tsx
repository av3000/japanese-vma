import React from 'react';
import LoginForm from '@/components/features/auth/Login';

const LoginPage: React.FC = () => {
	return <LoginForm heading="Welcome back!" buttonText="Log in" />;
};

export default LoginPage;
