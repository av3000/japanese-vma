// TODO(no issue): temporary legacy adapter because <reason>. Target: <v1 endpoint or generated client>. Remove when <condition>.

export type TemporaryLegacyResult = unknown;

export const fetchTemporaryLegacyResult = async (): Promise<TemporaryLegacyResult> => {
	throw new Error('Replace this template with the isolated legacy call.');
};
